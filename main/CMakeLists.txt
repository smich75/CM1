idf_component_register(
                    SRC_DIRS "." "ui" "support"
                    INCLUDE_DIRS "." "./ui" "./support"
                    )
# littlefs_create_partition_image(storage fs FLASH_IN_PROJECT)
